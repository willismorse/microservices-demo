---
apiVersion: v1
kind: Service
metadata:
  name: hostapp
  labels:
    app: hostapp
    chart: hostapp-0.1.0
    heritage: Tiller
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: hostapp
